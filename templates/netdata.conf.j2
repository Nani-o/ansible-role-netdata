{{ ansible_managed | comment }}

{% for section, configs in netdata_config.items() %}
[{{ section }}]
{% for key, value in configs.items() %}
        {{ key }} = {{ value }}
{% endfor %}
{% endfor %}
